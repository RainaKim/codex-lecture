# 5강 스피커 노트 — 작은 요청 하나를 plan-first로 통과시키기

Part 1 · Chapter 1 · 5강 · 실습 · 약 45분 · **슬라이드 없음** — root README를 띄우고 진행

## 이 노트 쓰는 법

- 5강은 이론 슬라이드가 없다. **왼쪽에 order-ops root `README.md`를 띄우고**, 오른쪽에 Codex를
  둔 split 화면으로 간다. 아래 오리엔테이션은 그 README를 위에서 아래로 훑는 대본이다.
- `[README §N]`은 그 섹션으로 스크롤해서 화면에 보이게 하라는 뜻이다. **가리킬 곳**은 굵게 적었다.
- 인용부호 안은 그대로 읽어도 되는 문장이다. ⚠️는 놓치면 뒤가 어긋나는 지점이다.

---

# 오리엔테이션 — repo부터 뜯어보기 (약 6분)

⚠️ **이 강의는 코드를 향해 가는 첫 강의다. 근데 바로 안 고친다. 「먼저 이 repo가 뭔지 보자」로
연다.** 2~4강 내내 notes·AGENTS.md만 만들었지 정작 `src/`를 제대로 연 적이 없다는 걸 짚어 준다.

⚠️ **여기서 실습을 미리 하지 않는다.** README에 「현재 관찰할 문제」가 꽤 자세히 적혀 있는데,
오리엔테이션에선 **가리키고 훑기만** 한다. 정확한 현재 동작 표·정책 결정·수정 범위는 실습에서
Codex로 한다. 여기서 다 풀면 실습이 김빠진다.

## 여는 말 (30초)

> "오늘부터 좀 달라집니다. 2강에선 대화를 정리했고, 3강에선 작업 경계를, 4강에선 기준을 파일로
> 만들었죠. 근데 그동안 우리가 정작 **`src/` 코드를 제대로 열어본 적이 없어요.** 오늘은 처음으로
> 실제 코드를 고치는 방향으로 갑니다."

> "근데 바로 고치진 않습니다. 그 전에, **우리가 앞으로 만질 이 repo가 뭐 하는 놈인지** 한번
> 뜯어봅시다. README부터 볼게요."

`[README 맨 위 — 제목 「order-ops — 개발자를 위한 Codex 워크플로 실습」을 띄운다]`

## A. Overview — 이건 완성된 앱이 아니다 (30초)

`[README §Overview로 스크롤]`

> "첫 문단 보세요. 주문 생성·결제·재고·관리자 조회가 연결된 작은 TypeScript API예요. 근데 그
> 아래 문장이 중요합니다."

**§Overview 둘째 문단 「완성된 서비스가 아닙니다 … 의도적으로 몇 가지 결함과 얕은 테스트를
남겨두었습니다」를 가리키며:**

> "**일부러 결함을 남겨 놨대요.** 완성품이 아니라, 현재 동작을 관찰하고 → 모호한 요구를 문서로
> 정리하고 → 정책 정하고 → 구현·검증하는 걸 연습하는 코드베이스입니다. 우리가 2~4강에서 한 게
> 딱 앞 두 단계였고, 오늘이 그다음이에요."

## B. 저장소 구조 — 우리가 만든 파일들이 여기 (1분)

`[README §2 저장소 구조 — 트리 다이어그램을 띄운다]`

**트리의 `src/` 블록을 가리키며:**

> "코드는 `src/` 다섯 개예요. `server.ts`가 Express 서버, `store.ts`가 메모리 저장소, 그리고
> `orders`·`payments`·`admin`. **오늘 우리가 볼 게 이 다섯 개 안에 다 있습니다.** 그 아래
> `tests/orders.test.ts`가 지금 있는 테스트 전부고요."

**트리 아래 「실습을 진행하면 다음 위치가 순서대로 생깁니다」 표(`docs/` · `notes/` ·
`.github/` · `AGENTS.md`)를 가리키며:**

> "여기 보세요. `notes/`랑 root `AGENTS.md`. **이게 여러분이 이미 만든 겁니다.** 2·3강에서
> `notes/` 파일들, 4강에서 `AGENTS.md`. 시작할 땐 없던 자리들인데, 우리가 하나씩 채워온 거예요.
> `docs/`는 6강, `.github/`는 8강에서 생깁니다."

⚠️ **여기서 4강을 회수한다. AGENTS.md가 이제 자동으로 읽힌다는 걸 한 번 더 못 박는다.**

> "그리고 4강에서 만든 이 `AGENTS.md`, 오늘 세션 열 때 Codex가 **시키지 않아도 먼저 읽습니다.**
> 오늘이 그게 실제로 작동하는 첫 강의예요."

## C. 앱 구조와 데이터 흐름 — 4강에 적은 게 실물로 (1분)

`[README §3 앱 구조와 데이터 흐름 — 첫 mermaid 다이어그램을 띄운다]`

**흐름도(Client → server → orders/payments/admin → store)를 가리키며:**

> "요청이 들어오면 `server.ts`가 받아서 `orders`·`payments`·`admin`으로 나눠 주고, 셋 다
> `store.ts` 메모리 저장소를 씁니다. 저장소 안엔 상품 Map, 주문 Map, 결제 시도 배열."

**§3 첫 문단 「별도 데이터베이스나 외부 결제 시스템 없이 메모리에서만 동작 … 서버를 다시
시작하면 … 사라집니다」를 가리키며:**

> "메모리에서만 돕니다. 서버 껐다 켜면 주문·결제 기록 다 날아가요. **이거 어디서 봤죠?** 4강에서
> AGENTS.md Overview에 우리가 적은 그 문장입니다 — 「메모리에 저장, 재시작하면 사라진다」.
> 그때 글로 적은 걸 오늘 실물로 보는 거예요."

*(여유 있으면 §3 「초기 상품」 표를 잠깐 — 키보드·마우스·모니터, 재고 5·10·2. "테스트할 때 이
상품들로 주문 넣습니다." 없으면 건너뛴다.)*

## D. 엔드포인트 — 그리고 결제 실패가 지금 어떻게 되나 (1.5분)

`[README §4 엔드포인트와 현재 동작 — 네 엔드포인트 표를 띄운다]`

**엔드포인트 표를 가리키며:**

> "API는 네 개예요. 주문 생성(`POST /orders`), 주문 조회, **결제(`POST /orders/:id/pay`)**,
> 관리자 목록. 오늘 우리가 파고들 건 세 번째, 결제입니다."

`[README §4 「POST /orders/:id/pay」 소절로 스크롤]`

**`cardToken` 설명 부분을 가리키며:**

> "실제 결제 시스템은 없어요. `cardToken` 문자열로 결과를 흉내 냅니다. `CARD-`로 시작하면 성공,
> `FAIL-`로 시작하면 실패."

**「실패하면 다음과 같이 동작합니다」 아래 1·2·3을 하나씩 가리키며:**

> "자, 여기가 오늘의 핵심이에요. **결제가 실패하면** — 첫째 실패 기록을 남기고, 둘째 **주문을
> 저장소에서 삭제하고**, 셋째 HTTP 200에 `{ ok: false }`를 반환합니다."

⚠️ **여기서 멈춘다. 이 세 줄이 오늘 요청의 씨앗이다. 천천히 짚는다.**

> "이상하지 않아요? **결제 실패했는데 200이에요.** 성공 코드죠. 그리고 **주문이 그냥 사라집니다.**
> '결제 실패'라는 상태로 남는 게 아니라, 아예 없어져요."

**그 아래 「현재 관찰할 문제」 세 줄을 가리키며(읽되 분석은 안 한다):**

> "README도 그렇게 적어 놨어요 — 실패한 주문이 사라진다, 실패랑 내부 오류를 구분하기 어렵다,
> 예상 못 한 예외도 `{ ok: false }`로 바뀐다. **지금은 여기까지만 봅니다.** 정확히 어느 코드가
> 어떻게 그러는지는 이따 Codex로 뜯어볼 거예요."

## E. 주문 상태 모델 — 이게 오늘 들어온 요청이다 (1분)

`[README §5 주문 상태 모델로 스크롤]`

**`type OrderStatus = 'pending' | 'paid' | 'cancelled'` 코드 줄을 가리키며:**

> "주문 상태가 코드에 세 개 선언돼 있어요. pending, paid, cancelled. **결제 실패 상태가
> 없죠?** 그리고 cancelled는 — 아래 표 보세요 — 선언만 돼 있고 실제로 들어갈 로직이 없어요."

**§5 「현재 빠져 있는 것」 목록에서 「결제 실패 주문의 상태와 보존 방식」을 가리키며:**

> "빠진 것 목록에 이게 있습니다 — **「결제 실패 주문의 상태와 보존 방식」.** 바로 이거예요.
> 오늘 우리한테 들어온 요청이 **「결제 실패 상태 개선」**입니다. 실패하면 주문이 사라지는 이걸,
> 제대로 된 상태로 남기자는 거죠."

**§5 마지막 문단 「즉시 임의 구현하는 문제가 아닙니다 … 정책을 확정한 다음 구현」을 가리키며:**

> "근데 README가 못을 박아 놨어요. **이걸 바로 구현하지 마라.** 현재 동작이랑 미결 질문을 먼저
> 기록하고, 정책을 정한 다음에 구현하라고. 그게 오늘 실습의 전부입니다."

## F. 오늘 목표로 전환 (30초)

`[README를 잠깐 접거나 그대로 둔 채]`

> "그래서 오늘 목표는 **결제 실패 처리를 고치는 게 아니에요.** 고치기 전에 **계획부터** 세웁니다 —
> 어디를 고칠지, 뭘 정해야 하는지, 어떻게 통과를 판정할지. **범위랑 검증 기준이 안 흔들리는
> 계획**을 구현 바로 직전까지 만드는 게 오늘입니다. 코드는 한 줄도 안 고쳐요."

> "그리고 아까 말한 그 AGENTS.md — Before Editing에 「읽을 것 읽고, 코드 말고 계획부터」라고
> 적었죠. **오늘 그게 우리를 대신 이끌어 줍니다.** 자, 시작해 볼까요."

*(첫 실습 E1 「관련 파일 후보 찾기」로 넘어간다 — 아래.)*

---

## 오리엔테이션에서 가리킬 곳 요약 (강사 치트시트)

| 순서 | README 위치 | 한마디 |
| --- | --- | --- |
| A | §Overview 둘째 문단 | 일부러 결함을 남긴 연습용 repo |
| B | §2 트리 `src/` + 아래 위치 표 | 코드 다섯 개 · notes/AGENTS.md는 우리가 만든 것 |
| C | §3 첫 mermaid + 「메모리에서만 동작」 | 4강 Overview에 적은 게 실물 |
| D | §4 `POST /pay` 실패 1·2·3 + 관찰 문제 | **실패 시 주문 삭제 + 200** — 오늘 요청의 씨앗 |
| E | §5 OrderStatus 3개 + 「결제 실패 주문의 상태와 보존」 | 결제 실패 상태가 없다 = 오늘 요청 |
| F | (README 접고) | 안 고친다, 계획부터, 구현 직전까지 |

---

# E1 — 관련 파일 후보 찾기 (약 10~12분)

3강 「요청 분해」의 첫 작업이 「관련 파일 후보 찾기」였다. **5강 E1이 그걸 진짜로 하는 자리다.**
산출물은 `notes/order-status-plan.md`의 `## 관련 파일 후보` 하나. 코드는 한 줄도 안 고친다.

⚠️ **이 단계에서 새 기능 두 개가 처음 나온다 — `@` mention 과 Plan mode.** 각각 한 줄로 눌러
주고 넘어간다. 깊게는 뒤 강의.

⚠️ **모델 tier**: 탐색은 Terra, **분류는 Sol**(관련 vs 수정 후보 판단이 무겁다), 기록은 Terra,
경로 존재·무변경 확인은 Luna.

## 여는 말

> "오리엔테이션에서 이 repo가 뭔지 봤죠. 이제 첫 단계예요. 근데 이거, 사실 **3강에서 이미
> 계획한 거**입니다. 3강 「요청 분해」 첫 줄 기억나세요? 「관련 파일 후보 찾기」였죠. 3강은 '뭘
> 할지' 적었고, 오늘은 그걸 하나씩 **실행**하는 겁니다."

`[왼쪽 화면을 lab 05 README로, 오른쪽을 Codex로]`

## 1. 새 Task 열고 요청 파일만 @로 붙이기

> "새 Task를 엽니다. 열자마자 AGENTS.md가 자동으로 붙어요 — 4강에서 만든 그거. 이제 세션마다
> 자동이에요."

⚠️ **`@` mention 을 처음 쓴다. 마크다운 링크 타이핑과 다르다는 걸 반드시 짚는다 (lab이 명시).**

> "입력창에 `@`를 칩니다. 파일 검색이 떠요. 거기서 `notes/order-status-request.md`를 골라요 —
> 2강에서 만든 요청 기록이죠. **손으로 마크다운 링크를 타이핑하는 거랑 다릅니다.** 그건 그냥
> 글자고, `@` mention은 Codex가 그 파일을 진짜 문맥에 넣는 거예요."

⚠️ **여기가 오늘의 핵심 장면. `src/` 파일을 왜 안 붙이는지 — 4강의 보상이다.**

> "여기서 중요한 거. **요청 파일 하나만 붙입니다.** `src/`나 `tests/`는 안 붙여요. 왜? Codex가
> 알아서 찾게 둘 거거든요. 어떻게 아냐고요? **AGENTS.md Before Editing에 「관련 테스트랑 코드를
> 읽어라」라고 우리가 적어놨잖아요.** 4강에서 만든 그 문장이, 지금 우리 대신 탐색을 이끕니다.
> 이게 4강을 왜 했는지에 대한 답이에요."

## 2. Plan mode로 영향 경로 탐색 (Terra)

⚠️ **Plan mode 를 처음 쓴다. 4강 Read Only와 이어서 설명한다.**

> "이제 Plan mode를 켭니다. `Shift+Tab` 또는 `/plan`. 4강에서 Read Only 모드 얘기했죠 — 파일을
> 못 고치는 모드. Plan mode는 거기서 한 발 더 나가서 **조사하고 계획을 제안까지** 합니다.
> 지금은 후보만 찾을 거라 딱 맞아요."

계약을 친다:

```
목표: 결제 실패 상태 개선에 필요한 관련 파일 후보와 선택 이유를 찾는다
문맥: mention으로 연결한 요청 기록, 자동 로드된 root AGENTS.md, repo에서 확인되는 실제 연결
제약: 읽기만 하고 정책·수정 내용을 확정하거나 어떤 파일도 수정하지 않는다
완료 조건: 후보마다 repo 상대 경로·현재 역할·요청과 연결되는 이유·근거 위치가 있다
```

⚠️ **결과가 나와도 바로 파일에 안 옮긴다. 연결이 이어지는지 먼저 본다.**

> "후보가 나왔죠. 바로 파일에 옮기지 마세요. 먼저 **연결이 이어지는지** 봅니다 — 요청 들어오는
> 데(server), 상태·저장(store), 성공·실패 처리(payments), 결과 보는 데(admin), 그리고 기존
> 테스트까지. 오리엔테이션에서 본 그 흐름이 후보에 다 잡혔는지."

## 3. 후보를 역할로 분류 (Sol)

> "후보가 충분하면 이제 분류합니다. **이게 이 실습의 진짜 배울 점이에요.**"

계약을 친다:

```
목표: 방금 찾은 후보를 기능 수정 후보·검증 변경 후보·탐색 전용·검사 범위 근거로 분류한다
문맥: 직전 후보 표와 각 파일의 실제 import·router·test 연결
제약: 새 정책이나 구체적인 변경 방법을 만들지 않고 분류 이유를 함께 적는다
완료 조건: 모든 후보에 분류와 유지·제외 이유가 있고 누락 가능성이 보고되어 있다
```

⚠️ **관련 ≠ 수정 후보. 이 구분이 E1의 알맹이다.**

> "관련 있다고 다 고칠 파일이 아니에요. `server.ts`는 라우터를 연결하니 관련은 있지만, 우리가
> 고칠 건 아닐 수 있죠 — **탐색 전용**입니다. 테스트는 **검증 바꿀 후보**고. 결제 실패 상태를
> 실제로 바꿀 `payments`·`store`가 **기능 수정 후보**. 이걸 안 나누고 다 「고칠 파일」로 묶으면,
> 다음 단계에서 수정 범위가 뭉개집니다."

## 사람 확인 — 후보 범위 (Human Gate)

⚠️ **3강 human gate가 여기서 실물로 온다. 사람이 확인하고서야 기록으로 넘어간다.**

lab README의 「사람 확인 — 후보 범위」 다섯 항목을 같이 본다:

- 상태 타입·저장 위치가 포함됐나
- 주문 생성·결제·조회·관리자 조회가 끊김 없이 연결됐나
- 기존 테스트가 검증 변경 후보로 들어갔나
- 앱 조립 파일·검사 설정을 기능 수정 대상으로 오해 안 했나
- 정책 선택이나 수정 방법이 후보 이유에 안 섞였나

> "누락이 있으면 **어느 역할이 빠졌는지만** 알려주고 다시 찾게 합니다. 파일명을 사람이 대신
> 채워주지 마세요. 그럼 실습이 아니라 받아쓰기가 돼요."

## 4. 확인된 후보만 기록 (Default mode · Terra)

> "범위를 확인했으면 Plan mode를 끄고 Default로 돌아와서, 확인된 것만 파일에 기록합니다."

계약을 친다:

```
목표: 확인된 후보를 notes/order-status-plan.md의 ## 관련 파일 후보에 기록한다
문맥: 이 Task에서 검토하고 승인한 후보와 분류만 사용한다
제약: repo 상대 경로와 선택 이유만 쓰고 정책·수정안은 쓰지 않으며 src와 tests는 수정하지 않는다
완료 조건: 모든 경로가 존재하고 후보마다 분류와 이유가 있으며 다른 파일 변경이 없다
```

⚠️ **라인 번호를 계획 문서에 안 쓴다. 왜인지 짚는다.**

> "채팅엔 「payments.ts 87번째 줄」 이렇게 나와요. 근데 계획 문서엔 **라인 번호를 안 씁니다.**
> 코드가 한 줄만 위로 밀려도 87번이 틀려지거든요. **경로랑 역할로** 써요 — 「payments.ts, 결제
> 실패 처리」. 코드가 움직여도 뜻이 안 변하게."

## 왜 이 순서인가 (구두로)

> "탐색 결과를 바로 파일에 저장하면, 그냥 관련만 있는 파일이랑 진짜 고칠 파일이 섞여요. 먼저
> 영향 경로 확인하고 → 사람이 범위 검토하고 → 기록하면, **E2에선 이 탐색을 다시 안 하고 현재
> 동작 확인에만 집중**할 수 있습니다. 3강에서 배운 「종료 상태가 있어야 다음이 깨끗하다」랑
> 같은 얘기예요."

## 검수 + 돌아다니며 잡을 것

lab README 검증 다섯 줄이 검수 항목이다:

- 후보 세 개 이상 · 각 후보에 이유 · 네 분류로 구분 · repo 상대 경로 실재 · plan 파일 외 무변경

자주 나오는 실수 (돌아다니며 잡는다):

- **요청 단어와 파일 이름만 비교** → "import랑 라우터 연결을 따라가야죠. 이름만 비슷한 거 말고."
- **관련 파일을 다 수정 대상으로 분류** → "`server.ts`도 고칠 거예요? 관련이지 수정 후보는 아니죠."
- **채팅의 절대 경로·라인 번호를 그대로 복사** → "경로랑 역할로. 라인은 곧 틀려져요."
- **원하는 상태·응답을 벌써 구현 방법처럼 적음** → "그건 E3에서 정책 정할 때. 지금은 후보만."

**엔딩 훅**

> "관련 파일이 뭔지, 그중 뭘 고칠지 후보가 나왔어요. 근데 이 파일들이 **지금 정확히 뭘 하고
> 있는지**는 아직 모르죠. 다음 단계에서 현재 동작을 표로 만듭니다."

*(다음 실습 대본은 lab README의 다음 단계가 나오면 이어서 작성.)*
